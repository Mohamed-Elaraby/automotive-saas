<style>
    body { font-family: dejavusans, sans-serif; font-size: 11px; color: #111827; line-height: 1.45; }
    h1, h2, h3, h4 { margin: 0 0 8px; font-weight: 700; }
    h1 { font-size: 20px; }
    h2 { font-size: 16px; }
    h3 { font-size: 13px; }
    p { margin: 0 0 6px; }
    table { width: 100%; border-collapse: collapse; margin-bottom: 10px; }
    thead { display: table-header-group; }
    th, td { border: 1px solid #d1d5db; padding: 6px; vertical-align: top; }
    th { background: #f3f4f6; font-weight: 700; }
    .muted { color: #6b7280; }
    .small { font-size: 9px; }
    .section { margin-bottom: 14px; }
    .grid-table td { width: 25%; }
    .avoid-page-break, .keep-together { page-break-inside: avoid; }
    .new-page { page-break-before: always; }
    .long-text { white-space: pre-wrap; word-wrap: break-word; overflow-wrap: break-word; }
    .badge { display: inline-block; padding: 2px 6px; border: 1px solid #9ca3af; border-radius: 3px; font-size: 9px; }
    .signature-box { height: 56px; border: 1px solid #d1d5db; padding: 8px; }
</style>
